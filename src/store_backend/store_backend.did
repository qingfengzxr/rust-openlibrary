type Profile = record {
    "name": text;
    "description": text;
    "keywords": vec text;
};

service : {
    "get": (text) -> (Profile) query;
    "update": (text, Profile) -> ();
    "search": (text) -> (opt Profile) query;
}