type Profile = record {
    "book_name": text;
    "description": text;
    "sections": vec text;
};

service : {
    "get": (text) -> (Profile) query;
    "update": (text, Profile) -> ();
    "search": (text) -> (opt Profile) query;
}